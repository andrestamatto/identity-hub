spring:
  application:
    name: identity-hub

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics


logging:
  level:
    org:
      springframework:
        security: DEBUG