# identityhub
IdentityHub is a modern, extensible authentication and authorization service built with Java and Spring Boot. It supports multiple identity providers, JWT-based security, and can be used as a standalone microservice or embedded as a module.
